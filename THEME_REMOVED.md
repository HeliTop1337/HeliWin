# Удаление переключателя темы

## Что было изменено

### 1. Убран ThemeToggle из Header
**Было:**
```
Logo  Навигация  [●27 Онлайн] [🌙] Баланс Профиль
```

**Стало:**
```
Logo  Навигация  [●27 Онлайн] Баланс Профиль
```

### 2. Зафиксирована темная тема
В `_app.tsx` тема всегда устанавливается как `dark`:

```typescript
useEffect(() => {
  const root = window.document.documentElement;
  root.classList.remove('light');
  root.classList.add('dark');
}, []);
```

### 3. Удалены импорты
- Удален импорт `ThemeToggle` из `Header.tsx`
- Удален импорт `useThemeStore` из `_app.tsx`

## Преимущества

✅ Больше места в хедере
✅ Упрощенный интерфейс
✅ Единый стиль (всегда темная тема)
✅ Меньше отвлекающих элементов

## Код изменений

### Header.tsx
```tsx
// Было
<div className="flex items-center gap-3">
  <OnlineCounter />
  <ThemeToggle />
  {isAuthenticated ? (
    // ...
  )}
</div>

// Стало
<div className="flex items-center gap-3">
  <OnlineCounter />
  {isAuthenticated ? (
    // ...
  )}
</div>
```

### _app.tsx
```tsx
// Было
const theme = useThemeStore((state) => state.theme);

useEffect(() => {
  const root = window.document.documentElement;
  root.classList.remove('light', 'dark');
  root.classList.add(theme);
}, [theme]);

// Стало
useEffect(() => {
  const root = window.document.documentElement;
  root.classList.remove('light');
  root.classList.add('dark');
}, []);
```

## Визуальный результат

### Хедер теперь выглядит так:
```
┌────────────────────────────────────────────────────┐
│ Logo  Кейсы  Crash  ...  [●27 Онлайн] Баланс      │
└────────────────────────────────────────────────────┘
```

Больше нет переключателя темы, освобождено место в хедере.

## Файлы, которые можно удалить (опционально)

Если переключатель темы больше не нужен нигде:
- `frontend/components/ThemeToggle.tsx`
- `frontend/store/useThemeStore.ts`

## Тестирование

1. Откройте сайт
2. Проверьте что тема всегда темная
3. Проверьте что в хедере нет переключателя темы
4. Проверьте что счетчик онлайн отображается корректно
